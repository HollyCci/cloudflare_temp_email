import { test } from '../../fixtures/test';

test.skip('access token interceptor tests depend on Vue module imports (/src/api/index.js, /src/store/index.js) that no longer exist in the React frontend', () => {});
